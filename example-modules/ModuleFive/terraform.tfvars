aws_access_key = "<insert access key here>"

aws_secret_key = "<insert secret key here>"

private_key_path = "<path to private key, use \\ for any \ in path>"

bucket_name = "<unique bucket name>"

arm_subscription_id = "<insert subscription id here>"

arm_principal = "<insert principal id here>"

arm_password = "<insert principal secret here>"

tenant_id = "<insert tenant id here>"
